const { User, Sickness, UserSickness } = require(`../models`)
const axios = require(`axios`)

class SicknessController {
    static getSickness(req, res, next) {
    }
}

module.exports = SicknessController